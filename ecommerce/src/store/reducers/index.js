const initialState = {
  products: [],
  categories: [],
  currentProduct: [],
};

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
